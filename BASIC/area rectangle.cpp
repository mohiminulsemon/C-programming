#include<stdio.h>
int main()
{
    int height,width,area;
    printf("Enter the height:");
    scanf("%d",&height);
    printf("Enter the width:");
    scanf("%d",&width);
    area=height*width;
    printf("the area is %d",area);
    return 0;
}
